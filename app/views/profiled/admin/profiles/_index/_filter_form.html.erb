<%= form_with theme: 'search', model: Profiled::Profile.new, url: { action: params[:action] } do |f| %>
  <div class="field-body">
    <%= f.text_field :name %>
    <%= f.text_field :identity %>
    <div class="field is-narrow">
      <%= f.submit %>
      <%= link_to t('.clear'), filter_params(except: [:name, :identity]), class: 'button is-light' %>
    </div>
  </div>
<% end %>
