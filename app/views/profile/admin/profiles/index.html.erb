<%= render layout: 'index_table', locals: { cache_key: Profile.column_names.hash } do %>
  <%= render partial: 'index_tbody', collection: @profiles, as: :profile %>
<% end %>

<%= paginate @profiles %>
